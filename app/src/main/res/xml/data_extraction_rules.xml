<?xml version="1.0" encoding="utf-8"?>
<!--
    Android 12+ backup/transfer rules. We back up the full app package so the
    user's persisted language, theme, and saved presets follow them to a new
    device.
-->
<data-extraction-rules>
    <cloud-backup />
    <device-transfer />
</data-extraction-rules>
