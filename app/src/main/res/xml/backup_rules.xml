<?xml version="1.0" encoding="utf-8"?>
<!--
    Auto Backup rules. We back up everything by default; the DataStore-backed
    settings + presets are small and the user opts in by installing Omerta.
-->
<full-backup-content />
